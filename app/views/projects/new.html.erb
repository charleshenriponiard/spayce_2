<div class="container" data-controller="bill">
  <h1 class="my-5"> <%= t('views.projects.new.title') %></h1>
  <div class="row">
    <div class="col-md-8">
      <div class="card-body mb-3 mb-md-5">
        <%= render "form", project: @project %>
      </div>
    </div>
    <div class="col-md-4 mb-3 mb-md-0">
        <%= render "bill", project: @project, confirmation: false %>
    </div>
  </div>
</div>
