
<div class="wrapper">
  <div class=" container">
    <div class="card-connexion">
      <h1 class="text-center mb-3">You want change your password ?</h1>
      <%= simple_form_for(resource, as: resource_name, wrapper: :inline_form, url: password_path(resource_name), html: { method: :put })  do |f| %>
        <%= f.error_notification %>
        <%= f.input :reset_password_token, as: :hidden %>
        <%= f.full_error :reset_password_token %>
        <div class="form-inputs">
          <div class="form-inputs">
            <%= f.input :password,
                        label: "New password",
                        required: true,
                        autofocus: true,
                        # hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                        input_html: { placeholder: "Password", autocomplete: "new-password", class:"xl-input" } %>
            <%= f.input :password_confirmation,
                        label: "Confirm your new password",
                        required: true,
                        input_html: { placeholder: "Confirme password", autocomplete: "new-password", class:"xl-input" } %>
          </div>
        </div>
        <div class="form-actions text-center">
          <%= f.button :submit, "Change my password", class: "btn btn-info button-connexion text-white" %>
        </div>
      <% end %>
      <div class="text-center mt-4">
        <div class="d-flex align-items-center justify-content-center">
          Don't have an account yet ? <%= link_to "Sign up", new_registration_path(resource_name), class: "btn btn-secondary ml-3" %>
        </div>
        <%= link_to "Log in", new_user_session_path, class: "nav-btn" %>
      </div>
    </div>
  </div>
</div>
